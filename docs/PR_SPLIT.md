# Pull request plan (betaflight/blackbox-log-viewer)

Contribute **to upstream** — not a fork product. UI must match `master` except one toolbar button.

| PR | Contents |
|----|----------|
| **1** | `src/diagnostics/*`, store, panel, `input_guard.js`, App.vue + AppToolbar wiring, modal overlay fix in `vite.config.js` |
| **2** | Vitest tests + `test.yml` |
| **3** | `docs/AUTO_DIAGNOSTICS.md`, `docs/UPSTREAM_INTEGRATION.md` |

**Out of scope:** Welcome page, navbar redesign, logos, colors, marketing README changes.

See [UPSTREAM_INTEGRATION.md](./UPSTREAM_INTEGRATION.md).
